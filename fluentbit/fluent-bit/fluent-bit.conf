[SERVICE]
    Flush        1
    Log_Level    info
    Parsers_File parsers.conf

[INPUT]
    Name         tail
    Path         /var/log/app/*.log
    Tag          app.*
    Read_from_Head true

[OUTPUT]
    Name         stdout
    Match        *
    Format       json_lines
