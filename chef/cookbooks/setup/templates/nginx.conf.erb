events {
  worker_connections 1024;
}

http {
  sendfile on;
  tcp_nopush on;

  server {
    listen 80 default_server;
    server_name _;

    location / {
      root /var/www/html;
      index index.html;
    }
  }
}
