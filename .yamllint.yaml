extends: relaxed

rules:
  line-length: disable
  document-start: disable
  truthy: disable
  new-lines: disable
  empty-lines: disable
  brackets: disable
  trailing-spaces: disable
  indentation: disable
