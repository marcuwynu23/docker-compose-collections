config:
  target: "http://localhost:3000"
  phases:
    - duration: 60   # run for 60 seconds
      arrivalRate: 10  # 10 virtual users per second

scenarios:
  - name: "Test root and health endpoints"
    flow:
      - get:
          url: "/"
      - get:
          url: "/api/health"